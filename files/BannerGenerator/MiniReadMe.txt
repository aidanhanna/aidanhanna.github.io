These are the python files I create to make the banner image at the top of the page.
They are inefficient but efficiency was not a priority as they only needed to run one time.
They rely on the pillow module found at https://python-pillow.org
